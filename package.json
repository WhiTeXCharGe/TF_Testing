{
  "name": "timefold-api-controller",
  "version": "1.0.0",
  "description": "Azure API Controller — Blob Storage + Batch backed replacement for the local service/ mock. Deployed to Azure Container Apps.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "@azure/identity": "^4.4.1",
    "@azure/storage-blob": "^12.24.0"
  }
}
