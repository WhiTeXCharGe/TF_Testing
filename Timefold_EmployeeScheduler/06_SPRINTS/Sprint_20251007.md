# 🚀 Sprint Plan – 2025/10/07

## 🎯 Sprint Goal
Focus on improving scheduling accuracy and logic robustness in both Pass 1 and Pass 2.  
Main targets are to refine overtime handling, validate manager assignment, and enhance skill-level balance.

---

## 📋 Planned Tasks
### 🔹 Pass 1
- [ ] Add new **overtime logic**
  - Allow limited overtime per block (≤ one extra day’s worth of hours).
  - Update `_auto_hours()` to select flexible hours from EnvConfig when required.
  - Validate no underfill across all operations.

### 🔹 Pass 2
- [ ] Implement **Manager Check**
  - Enforce at least one manager (`is_manager=True`) assigned per block.
  - Make sure this constraint applies only in Pass 2, not Pass 1.

### 🔹 General
- [ ] Add **Skill Level Balance**
  - Create soft constraint to distribute workload between employees of different skill levels more fairly.
  - Measure fairness by comparing total assigned hours.
  
---

## 🧪 Expected Outcome
- Overtime handled within defined safe limits.
- Every block includes at least one manager.
- Workload distribution more balanced across all employees.
- Excel results reflect correct workload and no cross-factory conflicts.

---

## 🧭 Notes
- Keep Pass 1 and Pass 2 constraints fully separated.
- Use safe `group_by(..., ConstraintCollectors.sum(lambda ...: 1))` pattern.
- Prepare mock YAML input for testing overtime and manager logic simultaneously.
