This is a much bigger ask than a checklist — you want the actual step-by-step walkthrough (like Phase 01-07) rewritten for the company subscription, ending with a real working API deployed to ACA and the webapp wired to it. Let me also check the actual contract the webapp expects, since I want the guide's API code to match exactly rather than guess.


Ran a command, created 12 files, read a file, used 24 tools
Now let me update the checklist file to point at this new guided series, and update its RBAC/verification sections to reference the new phase docs for consistency.


Read a file, edited a file, ran a command, used a tool
I've built the full guided series plus real working code. Here's what's new:

Code (in web/api-controller/) — a real Node/Express app, not just doc snippets:

src/server.js — implements exactly the contract your webapp already calls (runService.ts): POST /runSolver, GET /status/:runId, GET /download/:runId (streams the file directly, matching what the webapp expects), DELETE /run/:runId. Uses DefaultAzureCredential, so it authenticates via your az login locally and automatically switches to the Container App's Managed Identity once deployed — same code, no environment-specific branches.
package.json, Dockerfile, .dockerignore — ready to build and push.
Syntax-checked, ready to npm install.
Guided walkthrough (in webapp/azure/), one phase per file like your original 01-07, but built for "company already created it, I don't know the names" reality:

Azure-Company-01-Access-And-Resources.md — sign in, find every resource, record real names into one private env script
Azure-Company-02-RBAC.md — check/assign all 10 role assignments via portal
Azure-Company-03-Storage-Verify.md — prove Blob read/write/SAS works
Azure-Company-04-Register-Timefold-Image.md — build the Timefold Docker image from web/Timefold/, push to the real ACR
Azure-Company-05-Batch-Setup-And-Run.md — verify/create the pool+job, submit one manual task end-to-end
Azure-Company-06-API-Controller-Deploy.md — test the new code locally against real Azure, then build/push/deploy it to the real ACA app
Azure-Company-07-Webapp-Connect.md — flip webapp/.env.local to the real API URL, full click-through test
Each doc has exact commands, "what you should have at the end" checklists, and a troubleshooting table — same style as your originals, no resource names guessed anywhere (all sourced from one env script filled in during Phase 1). The checklist file now points to this series and stays as your fast-reference companion.

One thing worth knowing before tomorrow: while investigating, I found the current local service/server.js mock returns /download/:runId as a raw file stream (not JSON+SAS-link like your old Phase 6/7 docs assumed) — the new API controller code matches the real webapp contract, so no webapp changes are needed.
