node_modules/
dist/
dist-sse/
.vite/
*.local
.env
.env.*
# Not a secret — the default Azure URL baked into `electron:build` installers
# (see the file's own header comment). Everything else under .env.* stays
# ignored (local overrides, etc).
!.env.production
electron-dist/
release/
.worktrees/
.superpowers/
mock-blob/
cypress/screenshots/
cypress/videos/
.claude/