{
  "name": "timefold-api-controller-test",
  "version": "1.0.0",
  "description": "Pipeline-only test controller — no Timefold solver, no Docker/ACR dependency for the Batch task. Proves webapp -> API -> Blob -> Batch -> Blob -> API -> webapp works end to end, decoupled from solver correctness.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "@azure/identity": "^4.4.1",
    "@azure/storage-blob": "^12.24.0"
  }
}
